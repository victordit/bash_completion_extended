#!/bin/bash

# copy files to /etc/bash_completion.d/
sudo rm /etc/bash_completion.d/composer
sudo rm /etc/bash_completion.d/n98-magerun.phar

echo "Delete all files for completion extend, close session terminal and reopen"